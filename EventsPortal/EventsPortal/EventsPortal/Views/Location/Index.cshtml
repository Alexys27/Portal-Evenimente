@model IEnumerable<EventsPortal.Models.LocationModel>

@{
    ViewData["Title"] = "Locations";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<style>
    body {
        margin-top:50px;
        background-color: #343a40; /* Set your desired dark background color */
        color: #ffffff; /* Set text color */
        padding-top: 20px; /* Adjust padding as needed */
    }

    .container {
        margin-top: 20px; /* Adjust margin as needed */
    }

    section {
        background-color: #495057; /* Set your desired section background color */
        padding: 15px;
        border-radius: 8px;
        margin-bottom: 20px;
        display: flex; /* Use flexbox for layout */
        justify-content: space-between; /* Distribute content evenly */
        align-items: center; /* Center items vertically */
        cursor: pointer; /* Add cursor pointer for better user interaction */
    }

    .text-section {
        flex: 1; /* Take remaining space in the section */
    }

    h2 {
        color: #ffffff; /* Set heading text color */
        margin-bottom: 10px; /* Add margin to separate heading from description */
    }

    p {
        color: #ffffff; /* Set paragraph text color */
        margin-bottom: 10px; /* Adjust margin as needed */
    }

    .image-section {
        margin-left: 20px; /* Add margin to separate image from text */
    }

    .img-thumbnail {
        max-width: 100px;
        max-height: 100px;
    }

    .btn-group .btn {
        color: #ffffff; /* Set button text color */
        display: block; /* Display the button as a block element */
        margin-top: 10px; /* Add margin to separate buttons */
    }

    .details-btn {
        display: none; /* Initially hide the details button */
        margin-top: 10px; /* Add margin to separate details button from buttons */
    }
</style>

<script>
    // JavaScript to handle section click and trigger details action
    document.addEventListener("DOMContentLoaded", function () {
        var sections = document.querySelectorAll("section");

        sections.forEach(function (section) {
            section.addEventListener("click", function () {
                var detailsBtn = section.querySelector(".details-btn");
                if (detailsBtn) {
                    detailsBtn.click();
                }
            });
        });
    });
</script>

<div class="container">
    <h1 class="display-4">Locations</h1>
    @if (User.IsInRole("Admin"))
    {
        <p>
            <a asp-action="Create" class="btn btn-primary">Create New Location</a>
        </p>
    }


    @foreach (var item in Model)
    {
        <section>
            <div class="text-section">
                <h2>@Html.DisplayFor(modelItem => item.LocationName)</h2>
                <p>@Html.DisplayFor(modelItem => item.LocationAdress)</p>
            </div>
            <div class="image-section">
                @if (!string.IsNullOrEmpty(item.ImagePath))
                {
                    <img src="@item.ImagePath" alt="Location Image" class="img-thumbnail" />
                }
                else
                {
                    <p>No image available</p>
                }
            </div>
            @if (User.IsInRole("Admin"))
            {

                <div class="btn-group" role="group">
                    <a class="btn btn-primary" asp-action="Edit" asp-route-id="@item.LocationID">Edit</a>
                </div>
            }

            <a class="btn btn-secondary details-btn" asp-action="Details" asp-route-id="@item.LocationID">Details</a>
        </section>
    }
</div>
